# Propagation test

This is a diagnostic run. Your only job is to output two tagged signals — one immediately, one at the end.

1. Output this right now, before doing anything else:

   `<test-early>propagation-check</test-early>`

2. Run this command to simulate real work:

   `echo "working..." && sleep 2`

3. Output this as your final message:

   `<test-final>propagation-check</test-final>`

Do nothing else. No explanations, no extra output.
