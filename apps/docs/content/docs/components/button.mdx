---
title: Button
description: Triggers an action or event, such as submitting a form or opening a dialog.
---

## Installation

<Tabs defaultValue="cli">
  <TabsSurface>
    <TabsList>
      <TabsTab value="cli">CLI</TabsTab>
      <TabsTab value="manual">Manual</TabsTab>
    </TabsList>
  </TabsSurface>
  <TabsPanel value="cli">
    ```bash
    npx shadcn@latest add "https://ora-ui.com/r/button.json"
    ```
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      ```bash
      npm install @base-ui/react class-variance-authority
      ```

      <Step>Copy the component</Step>

      ```bash
      npx shadcn@latest add "https://ora-ui.com/r/button.json" --no-install
      ```

      <Step>Update the import path</Step>

      ```tsx
      import { cn } from '@/lib/utils';
      ```
    </Steps>

  </TabsPanel>
</Tabs>

## Usage

```tsx
import { Button } from '@/components/ui/button';

<Button>Button</Button>;
```

## Variants

The `variant` prop controls the visual style of the button.

<div className="flex gap-2">
  <Button variant="solid">Solid</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="surface">Surface</Button>
  <Button variant="soft">Soft</Button>
  <Button variant="ghost">Ghost</Button>
</div>

```tsx
import { Button } from '@/components/ui/button';

export default function ButtonVariants() {
  return (
    <div className="flex gap-2">
      <Button variant="solid">Solid</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="surface">Surface</Button>
      <Button variant="soft">Soft</Button>
      <Button variant="ghost">Ghost</Button>
    </div>
  );
}
```

## Theme

The `theme` prop applies a color scheme across all variants.

<div className="flex gap-2">
  <Button variant="solid" theme="gray">
    Gray
  </Button>
  <Button variant="solid" theme="accent">
    Accent
  </Button>
  <Button variant="solid" theme="destructive">
    Destructive
  </Button>
</div>

```tsx
import { Button } from '@/components/ui/button';

export default function ButtonTheme() {
  return (
    <div className="flex gap-2">
      <Button variant="solid" theme="gray">
        Gray
      </Button>
      <Button variant="solid" theme="accent">
        Accent
      </Button>
      <Button variant="solid" theme="destructive">
        Destructive
      </Button>
    </div>
  );
}
```

## With Icon

Icons can be placed alongside button text. The button handles icon sizing automatically.

<div className="flex gap-2">
  <Button>
    <PlusCircleIcon />
    Create New
  </Button>
  <Button variant="soft">
    <CloudArrowUpIcon />
    Upload
  </Button>
</div>

```tsx
import { CloudArrowUpIcon, PlusCircleIcon } from '@heroicons/react/16/solid';
import { Button } from '@/components/ui/button';

export default function ButtonWithIcon() {
  return (
    <div className="flex gap-2">
      <Button>
        <PlusCircleIcon />
        Create New
      </Button>
      <Button variant="soft">
        <CloudArrowUpIcon />
        Upload
      </Button>
    </div>
  );
}
```

## Icon Button

Use `size="icon"` for icon-only buttons.

<Callout variant="warning" title="Accessibility">
  Always provide an `aria-label` on icon-only buttons. Without visible text, screen readers have no
  way to describe the button's purpose.
</Callout>

<Button size="icon" aria-label="Add">
  <PlusCircleIcon />
</Button>

```tsx
import { PlusCircleIcon } from '@heroicons/react/16/solid';
import { Button } from '@/components/ui/button';

export default function ButtonIconButton() {
  return (
    <Button size="icon" aria-label="Add">
      <PlusCircleIcon />
    </Button>
  );
}
```

## Props

| Prop        | Type                                                     | Default   |
| ----------- | -------------------------------------------------------- | --------- |
| `variant`   | `"solid" \| "outline" \| "surface" \| "soft" \| "ghost"` | `"solid"` |
| `theme`     | `"gray" \| "accent" \| "destructive"`                    | `"gray"`  |
| `className` | `string`                                                 | —         |

All other props are forwarded to the underlying [Base UI Button](https://base-ui.com/components/button) primitive.
