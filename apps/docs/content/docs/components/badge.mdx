---
title: Badge
description: A small status indicator for labelling and categorising content.
---

<ComponentPreview name="badge-hero" />

## Installation

<Tabs defaultValue="cli">
  <TabsSurface>
    <TabsList>
      <TabsTab value="cli">CLI</TabsTab>
      <TabsTab value="manual">Manual</TabsTab>
    </TabsList>
  </TabsSurface>
  <TabsPanel value="cli">
    <CodeBlockTabs
      items={[
        { label: 'pnpm', value: 'pnpm', code: 'pnpm dlx shadcn@latest add "https://ora-ui.com/r/badge.json"' },
        { label: 'npm', value: 'npm', code: 'npx shadcn@latest add "https://ora-ui.com/r/badge.json"' },
        { label: 'bun', value: 'bun', code: 'bunx --bun shadcn@latest add "https://ora-ui.com/r/badge.json"' },
        { label: 'yarn', value: 'yarn', code: 'yarn dlx shadcn@latest add "https://ora-ui.com/r/badge.json"' },
      ]}
    />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      <CodeBlockTabs
        items={[
          { label: 'pnpm', value: 'pnpm', code: 'pnpm add @base-ui/react class-variance-authority' },
          { label: 'npm', value: 'npm', code: 'npm install @base-ui/react class-variance-authority' },
          { label: 'bun', value: 'bun', code: 'bun add @base-ui/react class-variance-authority' },
          { label: 'yarn', value: 'yarn', code: 'yarn add @base-ui/react class-variance-authority' },
        ]}
      />

      <Step>Copy the component</Step>

      <ComponentSource name="badge" />

      <Step>Update the import paths if necessary to match your project setup.</Step>
    </Steps>

  </TabsPanel>
</Tabs>

## Usage

```tsx
import { Badge } from '@/components/ui/badge';

<Badge>Badge</Badge>;
```

## Examples

### Variants

The `variant` prop controls the visual style of the badge.

<ComponentPreview name="badge-variants" select={['solid', 'outline', 'surface', 'soft']} />

### Theme

The `theme` prop applies a color scheme across all variants.

<ComponentPreview
  name="badge-theme"
  select={['gray', 'accent', 'destructive', 'warning', 'success']}
/>

### With icon

Icons can be placed inline alongside a label.

<ComponentPreview
  name="badge-with-icon"
  select={[
    { label: 'Leading', value: 'leading' },
    { label: 'Trailing', value: 'trailing' },
  ]}
/>

### Icon only

Use `size="icon"` for compact status indicators where space is limited.

<ComponentPreview name="badge-icon-only" />

### As a link

Pass `render={<a />}` to render the badge as an anchor element.

<ComponentPreview name="badge-as-link" />

## API Reference

### Badge

| Prop        | Type                                                            | Default     |
| ----------- | --------------------------------------------------------------- | ----------- |
| `variant`   | `"solid" \| "soft" \| "outline" \| "surface"`                   | `"soft"`    |
| `size`      | `"default" \| "icon"`                                           | `"default"` |
| `theme`     | `"gray" \| "accent" \| "destructive" \| "warning" \| "success"` | `"gray"`    |
| `render`    | `React.ReactElement`                                            | `<span>`    |
| `className` | `string`                                                        | —           |

All other props are forwarded to the rendered element.
