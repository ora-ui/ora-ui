---
title: Checkbox
description: A control that allows the user to toggle between checked and unchecked states.
---

<ComponentPreview name="checkbox-hero" />

## Installation

<Tabs defaultValue="cli">
  <TabsSurface>
    <TabsList>
      <TabsTab value="cli">CLI</TabsTab>
      <TabsTab value="manual">Manual</TabsTab>
    </TabsList>
  </TabsSurface>
  <TabsPanel value="cli">
    <CodeBlockTabs
      items={[
        { label: 'pnpm', value: 'pnpm', code: 'pnpm dlx shadcn@latest add "https://ora-ui.com/r/checkbox.json"' },
        { label: 'npm', value: 'npm', code: 'npx shadcn@latest add "https://ora-ui.com/r/checkbox.json"' },
        { label: 'bun', value: 'bun', code: 'bunx --bun shadcn@latest add "https://ora-ui.com/r/checkbox.json"' },
        { label: 'yarn', value: 'yarn', code: 'yarn dlx shadcn@latest add "https://ora-ui.com/r/checkbox.json"' },
      ]}
    />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      <CodeBlockTabs
        items={[
          { label: 'pnpm', value: 'pnpm', code: 'pnpm add @base-ui/react' },
          { label: 'npm', value: 'npm', code: 'npm install @base-ui/react' },
          { label: 'bun', value: 'bun', code: 'bun add @base-ui/react' },
          { label: 'yarn', value: 'yarn', code: 'yarn add @base-ui/react' },
        ]}
      />

      <Step>Copy the component</Step>

      <ComponentSource name="checkbox" />

      <Step>Update the import paths if necessary to match your project setup.</Step>
    </Steps>

  </TabsPanel>
</Tabs>

## Usage

```tsx
import { Checkbox } from '@/registry/ui/checkbox';

<label className="flex items-center gap-2">
  <Checkbox defaultChecked />
  <span>Accept terms and conditions</span>
</label>;
```

## Examples

### Default

A checkbox paired with a label using a native `label` element.

<ComponentPreview name="checkbox-default" />

### Variant

Two visual styles are available: `solid` fills the checkbox on check, `surface` uses a border treatment.

<ComponentPreview name="checkbox-variant" select={['solid', 'surface']} />

### Theme

Use the `theme` prop to apply a color theme. `gray` is the default; `accent` uses the brand color.

<ComponentPreview name="checkbox-theme" select={['gray', 'accent']} />

### Indeterminate

Set `indeterminate` to render a mixed state, useful for parent checkboxes in a group.

<ComponentPreview name="checkbox-indeterminate" />

## API Reference

For more information, see the Base UI [Checkbox API Reference](https://base-ui.com/react/components/checkbox#api-reference).
