import { {{pascalCase name}} } from '@/components/ui/{{name}}';

export default function {{pascalCase name}}Hero() {
  return (
    {{! Some compound components need children or additional imports — adjust as needed  }}
    <{{pascalCase name}}>
      {/* TODO: replace with meaningful default content */}
    </{{pascalCase name}}>
  );
}
