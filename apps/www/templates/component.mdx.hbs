---
title: {{sentenceCase name}}
description: {{description}}
---

<ComponentPreview name="{{name}}-hero" />

## Installation

<Tabs defaultValue="cli">
  <TabsSurface>
    <TabsList>
      <TabsTab value="cli">CLI</TabsTab>
      <TabsTab value="manual">Manual</TabsTab>
    </TabsList>
  </TabsSurface>
  <TabsPanel value="cli">
    <CodeBlockTabs
      items={[
        { label: 'pnpm', value: 'pnpm', code: 'pnpm dlx shadcn@latest add "https://ora-ui.com/r/{{name}}.json"' },
        { label: 'npm', value: 'npm', code: 'npx shadcn@latest add "https://ora-ui.com/r/{{name}}.json"' },
        { label: 'bun', value: 'bun', code: 'bunx --bun shadcn@latest add "https://ora-ui.com/r/{{name}}.json"' },
        { label: 'yarn', value: 'yarn', code: 'yarn dlx shadcn@latest add "https://ora-ui.com/r/{{name}}.json"' },
      ]}
    />
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      <CodeBlockTabs
        items={[
          { label: 'pnpm', value: 'pnpm', code: 'pnpm add @base-ui/react' },
          { label: 'npm', value: 'npm', code: 'npm install @base-ui/react' },
          { label: 'bun', value: 'bun', code: 'bun add @base-ui/react' },
          { label: 'yarn', value: 'yarn', code: 'yarn add @base-ui/react' },
        ]}
      />

      <Step>Copy the component</Step>

      <ComponentSource name="{{name}}" />

      <Step>Update the import paths if necessary to match your project setup.</Step>
    </Steps>

  </TabsPanel>
</Tabs>

## Usage

{/* TODO: customise import and usage snippet */}

```tsx
import { {{pascalCase name}} } from '@/components/ui/{{name}}';

<{{pascalCase name}}>{{pascalCase name}}</{{pascalCase name}}>;
```

## Examples

{{#each variants}}
### {{sentenceCase this}}

{/* TODO: add description for the {{this}} variant */}

<ComponentPreview name="{{@root.name}}-{{this}}" />

{{/each}}
## Props

{/* TODO: document props from the component source */}

| Prop | Type | Default |
| ---- | ---- | ------- |
| | | |
