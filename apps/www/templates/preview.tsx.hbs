import { {{pascalCase name}} } from '@/components/ui/{{name}}';

export default function {{pascalCase name}}{{pascalCase variant}}() {
  return (
    <div className="flex gap-2">
      {/* TODO: replace with {{variant}} variant content */}
    </div>
  );
}
