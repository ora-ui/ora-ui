---
title: Button Group
description: Groups related buttons together with shared borders and consistent styling.
---

<ComponentPreview name="button-group-hero" />

## Installation

<Tabs defaultValue="cli">
  <TabsSurface>
    <TabsList>
      <TabsTab value="cli">CLI</TabsTab>
      <TabsTab value="manual">Manual</TabsTab>
    </TabsList>
  </TabsSurface>
  <TabsPanel value="cli">
    ```bash
    npx shadcn@latest add "https://ora-ui.com/r/button-group.json"
    ```
  </TabsPanel>
  <TabsPanel value="manual">
    <Steps>
      <Step>Install dependencies</Step>

      ```bash
      npm install @radix-ui/react-slot class-variance-authority
      ```

      <Step>Copy the component</Step>

      <ComponentSource name="button-group" />

      <Step>Update the import path</Step>

      ```tsx
      import { cn } from '@/registry/lib/utils';
      ```
    </Steps>

  </TabsPanel>
</Tabs>

## Usage

```tsx
import { ButtonGroup, ButtonGroupSeparator, ButtonGroupText } from '@/registry/ui/button-group';
import { Button } from '@/registry/ui/button';

<ButtonGroup>
  <Button variant="surface">One</Button>
  <Button variant="surface">Two</Button>
  <Button variant="surface">Three</Button>
</ButtonGroup>;
```

## Default

A horizontal group of buttons sharing a continuous border.

<ComponentPreview name="button-group-default" />

## Vertical

Use `orientation="vertical"` to stack buttons.

<ComponentPreview name="button-group-vertical" />

## With icons

Buttons in a group support icons the same way standalone buttons do.

<ComponentPreview name="button-group-with-icons" />

## Icon buttons

Groups of icon-only buttons for compact action bars.

<ComponentPreview name="button-group-icon-buttons" />

## With separator

Use `ButtonGroupSeparator` to divide buttons. Recommended for `solid` and `soft` variants — not needed for `outline`, which relies on its border for separation.

<ComponentPreview name="button-group-with-separator" />

## With text

Use `ButtonGroupText` to attach a non-interactive label to the group.

<ComponentPreview name="button-group-with-text" />

## Props

### ButtonGroup

| Prop          | Type                         | Default        |
| ------------- | ---------------------------- | -------------- |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |
| `className`   | `string`                     | —              |

All other props are forwarded to the underlying `<div>` element.

### ButtonGroupSeparator

| Prop          | Type                         | Default      |
| ------------- | ---------------------------- | ------------ |
| `orientation` | `"horizontal" \| "vertical"` | `"vertical"` |
| `className`   | `string`                     | —            |

All other props are forwarded to the [Separator](/docs/components/separator) primitive.

### ButtonGroupText

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | —       |

All other props are forwarded to the underlying `<div>` element.
